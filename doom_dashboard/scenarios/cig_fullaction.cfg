# Full-action elimination scenario on richer CIG maps (map01/map02).
# This keeps 1v1 multiplayer compatibility while exposing a broad control set.

doom_scenario_path = cig.wad

episode_timeout = 25200
screen_resolution = RES_320X240
render_hud = false

available_buttons =
    {
        ATTACK
        USE
        SPEED
        STRAFE

        MOVE_RIGHT
        MOVE_LEFT
        MOVE_BACKWARD
        MOVE_FORWARD
        TURN_RIGHT
        TURN_LEFT

        SELECT_WEAPON1
        SELECT_WEAPON2
        SELECT_WEAPON3
        SELECT_WEAPON4
        SELECT_WEAPON5
        SELECT_WEAPON6

        SELECT_NEXT_WEAPON
        SELECT_PREV_WEAPON

        LOOK_UP_DOWN_DELTA
        TURN_LEFT_RIGHT_DELTA
        MOVE_LEFT_RIGHT_DELTA
    }

available_game_variables =
    {
        FRAGCOUNT
        KILLCOUNT
        DEATHCOUNT
        HITCOUNT
        HITS_TAKEN
        DAMAGECOUNT
        DAMAGE_TAKEN
        HEALTH
        ARMOR
        SELECTED_WEAPON
        SELECTED_WEAPON_AMMO
    }

mode = ASYNC_PLAYER
