# Deathmatch with monsters AND full weapon controls.
# Same as deathmatch_compact but adds weapon-select buttons.

doom_scenario_path = deathmatch.wad
doom_skill = 3

screen_resolution = RES_320X240
render_hud = false

episode_start_time = 1
episode_timeout = 4200

available_buttons =
    {
        ATTACK
        USE
        SPEED
        STRAFE
        MOVE_RIGHT
        MOVE_LEFT
        MOVE_BACKWARD
        MOVE_FORWARD
        TURN_RIGHT
        TURN_LEFT
        SELECT_WEAPON1
        SELECT_WEAPON2
        SELECT_WEAPON3
        SELECT_WEAPON4
        SELECT_WEAPON5
        SELECT_WEAPON6
        SELECT_NEXT_WEAPON
        SELECT_PREV_WEAPON
    }

available_game_variables =
    {
        FRAGCOUNT
        KILLCOUNT
        DEATHCOUNT
        HITCOUNT
        HITS_TAKEN
        DAMAGECOUNT
        DAMAGE_TAKEN
        HEALTH
        ARMOR
        SELECTED_WEAPON
        SELECTED_WEAPON_AMMO
    }

mode = PLAYER
